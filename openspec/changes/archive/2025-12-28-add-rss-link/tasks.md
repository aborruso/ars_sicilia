# Tasks: Add RSS Link to Header

## Implementation

- [x] **Add RSS link to Header navigation**
  - Open `src/components/layout/Header.astro`
  - Locate the `<ul class="flex gap-6 items-center">` navigation list
  - Add new `<li>` item after "Sito ARS" link
  - Insert `<a>` tag pointing to `/ars_sicilia/rss.xml`

- [x] **Add RSS icon**
  - Use emoji 📡 for simplicity (no SVG needed)
  - Or use simple inline SVG RSS icon if preferred
  - Apply RSS standard orange color: `text-orange-600` or `#FF6600`
  - Ensure icon is sized appropriately (same as text links)

- [x] **Add accessibility attributes**
  - Add `aria-label="Feed RSS"` to the link
  - Add `title="Feed RSS"` for tooltip on hover
  - Ensure link is keyboard-navigable (default `<a>` behavior)

- [x] **Apply consistent styling**
  - Use same classes as other nav links: `font-medium transition-colors`
  - Add hover effect: `hover:text-blue-600` or keep orange
  - Ensure spacing matches: parent `<ul>` has `gap-6`

- [x] **Set link attributes**
  - Set `href="/ars_sicilia/rss.xml"`
  - Add `target="_blank"` to open in new tab (optional, user preference)
  - Add `rel="alternate"` and `type="application/rss+xml"` (semantic)

## Validation

- [x] **Visual verification**
  - Run `npm run dev`
  - Check header on homepage
  - Verify RSS icon appears next to navigation links
  - Check icon color (orange or matching site theme)
  - Hover over icon and verify tooltip shows

- [x] **Functionality test**
  - Click RSS icon
  - Verify browser navigates to `/ars_sicilia/rss.xml`
  - Verify XML feed renders or prompts RSS subscription
  - Test on different browsers (Chrome, Firefox, Safari)

- [x] **Accessibility test**
  - Test keyboard navigation (Tab to RSS link)
  - Test with screen reader (verify aria-label is announced)
  - Verify link has visible focus indicator
  - Run Lighthouse accessibility audit

- [x] **Responsive design test**
  - Test on mobile viewport (375px, 414px)
  - Verify icon is visible and tappable
  - Verify touch target is at least 44x44px
  - Test on tablet (768px)
  - Test on desktop (1280px, 1920px)
  - Ensure icon doesn't break header layout

- [x] **Build test**
  - Run `npm run build`
  - Verify build succeeds
  - Check generated HTML in `dist/`
  - Run `npm run preview` and test built site

## Dependencies

None - standalone component change.

## Notes

- RSS feed already exists at `/ars_sicilia/rss.xml` (generated by `src/pages/rss.xml.ts`)
- No need to modify feed generation logic
- Consider using `target="_blank"` so users don't lose their place when opening RSS (debatable - some prefer same tab)
- If emoji 📡 doesn't render well, can use simple SVG:
  ```html
  <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" viewBox="0 0 16 16">
    <path d="M2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2zm1.5 2.5c5.523 0 10 4.477 10 10a1 1 0 1 1-2 0 8 8 0 0 0-8-8 1 1 0 0 1 0-2zm0 4a6 6 0 0 1 6 6 1 1 0 1 1-2 0 4 4 0 0 0-4-4 1 1 0 0 1 0-2zm.5 7a1.5 1.5 0 1 1 0-3 1.5 1.5 0 0 1 0 3z"/>
  </svg>
  ```
